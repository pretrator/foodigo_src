<input type="text" name="{{ item.name }}" 
data-grid="{{ item.grid }}" 
data-prefix="{{ item.prefix }}" 
data-double="{{ item.double }}" 
data-min="{{ item.min ? item.min : 0 }}" 
data-max="{{ item.max ? item.max : 1000 }}" 
id="{{ item.name }}" value="{{ item.value }}" class="form-control pa-rangeslider-input" />