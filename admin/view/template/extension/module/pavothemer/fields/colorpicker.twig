<div   class="input-group colorpicker-component pavo-colorpicker colorpicker-element" title="Using data-color attribute in the input" data-mode="{{ item.mode }}">

	<input type="text" name="{{ item.name }}" class="form-control" value="{{ item.value | escape }}" data-color="{{ item.value | escape }}" />
	<span class="input-group-addon"><i></i></span>
</div>